---
export interface Props {
  className?: string;
}

const { className } = Astro.props;
---
<div class={`card ${className}`}>
  <slot/>
</div>

<style>
.card {
  @apply flex flex-col flex-wrap rounded-md;
}
</style>
